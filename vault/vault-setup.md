# Vault setup

- Store non-production credentials in Vault.
- Use a dedicated policy for the reporting workload.
- Reference secrets via environment variables in Jenkins and Kubernetes.
- Never commit real secrets to the repository.
